# Contributing

This project is still in early design. Keep changes small, testable, and safety-first.

Before submitting changes:

- Run `npm run check`.
- Add or update tests for command construction, packet generation, parsing, or safety behavior.
- Keep stdout reserved for MCP protocol messages.
- Document any new write or destructive tool in `docs/safety.md`.
